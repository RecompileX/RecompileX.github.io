# RecompileX portfolio

Approved in chat: a dark terminal-inspired portfolio with lime accents, Hamza Kamal's name, the exact description “Building systems from the ground up.”, and four public GitHub projects. Use a responsive static site hosted with GitHub Pages. Include Wildfire Simulator, Toolbox C++, Breakout++, and Pong++, with descriptions grounded in their public READMEs. Use a real wildfire screenshot and CSS illustrations for the remaining projects. Avoid invented credentials, contact information, and private repositories.

Prepare `domains/recompilex.json` in the user's is-a-dev fork with a CNAME to `recompilex.github.io`. Never open an upstream pull request. Keep GitHub Pages serving its default hostname until domain approval; do not set a premature custom domain that would interrupt the preview.

Accessibility: semantic sections, keyboard navigation, visible focus, sufficient contrast, responsive layout, and reduced-motion support. No runtime dependencies or tracking.
